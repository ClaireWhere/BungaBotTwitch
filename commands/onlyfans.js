module.exports = {
    data: {
        name: 'onlyfans',
        description: '',
        defaultPermissions: false
    },
    execute(client, channel, tags, args) {
        client.say(channel, `*bonk* go to horny jail!`);
    }
};